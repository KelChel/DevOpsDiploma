# Deploy

Каталог для DevOps-конфигурации.

План для следующих итераций:

- Dockerfile для backend;
- Dockerfile для frontend;
- `docker-compose.yml` в корне репозитория;
- smoke-тесты;
- CI/CD workflow.

